name: Update Sponsors README

on:
  workflow_dispatch: # Permite ejecutarlo manualmente desde la pestaña Actions
  schedule:
    - cron: '0 0 * * *' # Se ejecuta automáticamente cada día a medianoche

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Generate Sponsors 💖
        uses: JamesIves/github-sponsors-readme-action@v1
        with:
          token: ${{ secrets.GH_TOKEN }}
          file: 'README.md'
          # Configuración visual (puedes ajustar esto luego)
          markers: 'true'
          minimum: 100 # Opcional: Si quisieras mostrar solo a los que pagan mínimo X centavos
          columns: 6   # Número de avatares por fila
          width: 80    # Tamaño del avatar en px
